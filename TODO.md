# TODO

- [x] Remove the hero-section image from the main landing page (both `crownpalm/src` and `crownpalm/red-palm-vision/src`).
- [ ] Adjust hero layout/styles so text is correctly positioned without the image.
- [ ] Run the dev/build (or at least typecheck) for both apps to ensure no TS/React errors.
- [ ] Verify hydration mismatch warnings after hero layout changes.


